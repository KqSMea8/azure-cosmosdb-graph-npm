#!/usr/bin/env node

// $ nodemon bin/www
// Chris Joakim, Microsoft, 2019/06/04

var app = require('../app');

app.set('port', process.env.PORT || 80);

var server = app.listen(app.get('port'), function() {
  console.log('Express server listening on port ' + server.address().port);
});
